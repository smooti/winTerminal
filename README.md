# winTerminal

### Installation

1. Launch Windows Terminal and then select the small downward-facing arrow in the title bar.
2. Select `Settings`
3. At the bottom left click `Open JSON file` and the settings.json file will open in your default text editor.
4. Copy the contents of this repos settings.json and paste it into your settings.json file